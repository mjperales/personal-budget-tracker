# Server Configuration
NODE_ENV=development
PORT=3000

# CORS (optional - defaults to localhost)
# CORS_ORIGIN=http://localhost:5173

# Mock Data (optional - set to "true" to seed transaction data on startup)
SEED_DATA=false
